import { {{schema.name}}Repository } from '{{importPaths.serviceToContract}}';
{{#each schema.methods}}
import type { {{input}}, {{output}} } from '@transport/gateway/gRPC/stubs/{{../descriptor}}';
{{/each}}

export class {{schema.name}}Service {
  constructor(private readonly repo: {{schema.name}}Repository) {}

{{#each schema.methods}}
  {{name}}(request: {{input}}): Promise<{{output}}> {
    return this.repo.{{name}}(request);
  }

{{/each}}
}