{{#each schema.methods}}
import type { {{input}}, {{output}} } from '@transport/gateway/gRPC/stubs/{{../descriptor}}';
{{/each}}

export interface {{schema.name}}Repository {
{{#each schema.methods}}
  {{name}}(input: {{input}}): Promise<{{output}}>;
{{/each}}
}
